services:
  mqtt:
    image: eclipse-mosquitto:2
    container_name: face_lock_mqtt
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./backend/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
