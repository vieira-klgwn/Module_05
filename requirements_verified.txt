# requirements_verified.txt
# Verified working on: Linux (kali) / Python 3.13.5 (anaconda) / 2026-06-12
# All versions below were imported and exercised successfully on this machine.
#
# Install:  pip install -r requirements_verified.txt
#
# NOTE on MediaPipe + Python 3.13:
#   mediapipe 0.10.32 imports but exposes NO `mp.solutions` on Python 3.13.
#   The pipeline AUTOMATICALLY falls back to bbox-based 5-point landmarks,
#   so the system still runs end-to-end. For best alignment quality use
#   Python 3.11 where `mediapipe==0.10.21` provides full FaceMesh.

# --- Computer Vision / Embeddings ---
opencv-python==4.10.0.84
numpy==2.1.3
onnxruntime==1.23.2
scipy==1.15.3
tqdm==4.67.1
mediapipe==0.10.32            # optional FaceMesh; bbox fallback if unavailable

# --- Messaging (MQTT) ---
paho-mqtt==2.1.0

# --- Backend / WebSocket relay ---
websockets==16.0
fastapi==0.136.3              # optional (alternative backend / future REST)
uvicorn==0.49.0               # optional (ASGI server for FastAPI)

# --- Serial (real ESP boards over USB) ---
pyserial==3.5

# --- Optional web framework (not required by the demo) ---
flask==3.1.0
